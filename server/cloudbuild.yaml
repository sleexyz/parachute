steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  args:
  - gcloud
  - functions
  - deploy
  - register_activity_refresh
  - --region=us-central1
  - --source=.
  - --trigger-http
  - --runtime=nodejs20